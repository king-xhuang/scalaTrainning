import scala.io.Source

object phoneNumberToWords {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(81); 
  val in = Source.fromURl("");System.out.println("""in  : <error> = """ + $show(in ))}
}
